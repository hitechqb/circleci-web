version: 2.1
orbs:
  node: circleci/node@4.7

workflows:
  sample:
    jobs:
      - node/test:
          version: '16.10'
          # If you are using yarn, change the line below from "npm" to "yarn"
          pkg-manager: npm
